import { Reviewactivitys } from "../../../models/reviewActivity/reviewActivity";
import { Request, Response } from "express";

export const getReviewactivity = async (req: Request, res: Response) => {
  try {
  } catch (error) {}
};
